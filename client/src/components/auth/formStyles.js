import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
  root: {
    
  },
  formHeader: {
    fontSize: '2.5em',
    fontWeight: 'bold',
    marginBottom: '2em'
  },
  submitFormBtn: {
    display: 'block',
    margin: '0 auto'
  }
}));

export default useStyles;